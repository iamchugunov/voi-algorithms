function [flag] = is_match_traj(traj, poit, config)
    % 1. Берем последнюю точку траектории
    % 2. Рассчитываем расстояние между новой точкой и последней (2D или 3D)
    % 3. Сравниваем полученную норму с каким-то порогом
    % 4. Откуда взять порог
end